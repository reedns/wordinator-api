== README

This is a simple api for determining if two words are an anagram.  Simply add two words as paramaters, e.g. localhost:3000/anagrams/word1/word2 and it will return true or false based on whether or not they are an anagram.  The page will render the response using JSON.
